axios.get('https://jsonplaceholder.typicode.com/todos/1').then((data)=>{
    const posts = data.posts;
    // 1. Loop the posts array
    // 2. Create a div with class name of .card
    // 3. Create a H1 tag and add the post content to the tag with a background style of yellow
    // 4. Append H1 to to the Div
    // 5. Append the div to the root element in HTML
})
